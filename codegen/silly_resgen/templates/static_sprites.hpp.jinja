// 这个文件是自动生成的，别改。

#include <silly_framework/utilities/sprite2d.hpp>

namespace sprites {
{% for sprite in sprites %}
const silly_framework::sprite2d& {{sprite.name}}();
{% endfor %}
} // namespace sprites

